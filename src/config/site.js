/**
 * Site-level metadata — who made this, as opposed to what it teaches.
 *
 * Course content lives in `course.js`; this file is only about attribution.
 * The navbar renders `AUTHOR` through `components/AuthorBadge.jsx`.
 */

export const AUTHOR = {
  handle: 'munna-68',
  github: 'https://github.com/munna-68',
}
