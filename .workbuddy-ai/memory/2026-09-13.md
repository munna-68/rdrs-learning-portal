# 2026-09-13

## Built: RDRS Learning Portal (initial build)

Built the complete app from the supplied spec — Vite + React + Tailwind, no backend,
localStorage as the only persistence. 11 video modules, YouTube IFrame API watch-time
tracking, 80% unlock threshold, client-side PDF certificate.

Key decisions:
- Watch time accumulates **wall-clock seconds while PLAYING**, not playhead position,
  so seeking forward earns nothing. 1s poll, 3s cap per tick, clamped to video duration.
- `estimatedDuration` per module is only a fallback denominator until the player reports
  the real duration; then it's discarded. Keeps the overall bar meaningful on first visit.
- Certificate is real DOM at 1123×794, kept mounted off-screen, rasterised at 2× with
  `html2canvas-pro`, placed on a landscape A4 page with `jsPDF`. Both libs are dynamic
  imports so the initial bundle stays ~55 kB gzipped.
- Tailwind pinned to **v3.4** deliberately — v4 emits `oklch()` which plain html2canvas
  can't parse.
- Unlocking is a one-way door: once `completedAt` is stamped, a later more accurate
  duration reading can't re-lock an earned certificate.

Verification: built a CDP harness driving the real production bundle in headless Chrome.
46 checks green on both dev and prod. Real YouTube playback banked ~8s of watch time in
~9s. Downloaded PDF verified at 1 page, MediaBox 841.89 × 595.28 pt.

## Added: author badge in the navbar

Requested by munna-68 — small, non-prominent GitHub attribution in the top navbar.

- New `src/config/site.js` holds `AUTHOR = { handle: 'munna-68', github: '...' }`.
  Deliberate split: `course.js` is content, `site.js` is attribution.
- New `src/components/AuthorBadge.jsx` — muted `text-slate-400`, 12px, icon + handle,
  `target="_blank"` with `rel="noreferrer noopener"`, aria-label for screen readers.
  Handle text collapses to icon-only below `sm` so it never crowds a phone navbar.
- Placed in both navbars: `Dashboard.jsx` (left of the reset control) and `VideoView.jsx`
  (right of the module counter).

Verification: 32 dedicated checks green — link target/rel/label, colour is genuinely
muted (`rgb(148,163,184)`, not the accent), no collision with neighbouring controls, no
horizontal overflow at 360/375/390/430 px, and the handle collapses correctly on mobile.
Full 46-check regression suite re-run afterwards: still green.

One self-correction worth noting: I first asserted the navbar height was "unchanged at
64px" and it failed at 69px. An A/B probe (same page, badge hidden vs shown) proved the
header is 69px **either way** — 64px was a number I had invented, not a baseline. The
badge is 28px inside a 36px row and changes nothing. Replaced the bogus assertion with
the real invariant: adding the badge must not increase the navbar height.
